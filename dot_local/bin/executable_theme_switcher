#!/usr/bin/env bb

(require '[babashka.process :refer [shell]])

(defn get-current-mode []
    (shell {:out :string} "defaults read -g AppleInterfaceStyle 2>/dev/null"))

(defn set-theme [mode]
    (if (= mode "Dark")
        


